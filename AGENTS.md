# ZcVersionBox 工程约定

- 沿用用户已有的文件命名和目录结构。C++ / Qt Designer 文件名使用小写，模块拆分遵循已有下划线前缀，例如 `homepage_page_backup.cpp`；类名使用 Qt 风格的 PascalCase。
- 已有文件保留原名和大小写，包括 `GlobalConstants.h`、`CMakeLists.txt`。修改 UI 或功能时，不顺带改换命名体系或重组顶层目录。
- 主窗口位于 `windows/mainwindow.*`，子页面位于 `windows/mainwindow_child/{homepage,settingpage,aboutpage}/`；首页分区使用 `homepage/pages/homepage_page_*`，备份条目展示使用 `homepage/trackfiles/`。新增文件就近放入原模块。
- 通用业务、监控、配置和文件辅助放在 `utils/`，窗口导航、主题和通知辅助放在 `windows/mainwindow_*`。保持页面、服务、模型和监控生命周期独立。
- 保留 Designer、标准 Qt Widgets 与 QlementineStyle；Switch、Expander、LoadingSpinner、Popover 等增强控件按实际用途使用。

## Git 工作流

- 每个新功能开始前，先检查工作区和当前分支，再创建并切换到独立功能分支，不在主分支直接开发新功能。继续同一功能或修复其验收问题时，复用该功能分支。
- 分支名沿用仓库已有习惯，默认使用 `feature/<功能简述>`，不使用 `codex` 前缀。
- 按可独立说明和回退的阶段进行本地提交，不把整个功能留到最后才一次性提交。仅提交当前任务相关改动，提交信息说明本阶段完成的内容；验证范围遵循下方“验证与测试”约定，不因提交而额外运行全量测试。
- 新功能的建分支和本地阶段性提交已获用户授权，直接执行，无需逐次确认；不得覆盖或混入用户已有的无关改动。
- 开发完成后，汇总功能改动、实际验证结果和验收步骤，保留在功能分支等待用户验收。用户明确确认该功能验收通过前，不推送或合并。
- 用户明确确认验收通过后，即视为授权推送和合并：推送功能分支，合并到目标分支（默认 `main`，另有指定时遵从指定），并推送合并结果，无需再次询问相同授权。

## 验证与测试

- 按改动的实际影响选择最小且足够的验证范围，不默认每次修改都执行完整构建或全量测试。
- 文档、注释、文案、颜色、间距等低风险调整，以内容和差异检查为主；涉及界面时按需增量编译或检查相关页面，不运行无关业务回归，不为这类可逆调整新增自动化测试。
- 局部功能修改优先运行相关 Qt Test 测试函数；需要整组验证时，再运行对应的 CTest 测试组（`regression` 或 `backup_core`），不默认两组全跑。
- 涉及备份、恢复、文件删除、Git 操作、监控状态或跨模块重构时，按影响扩大回归范围，必要时运行全量测试。发布打包前遵循 `CONTRIBUTING.md` 的完整构建与测试要求。
- 优先复用已有构建目录并增量构建。检查通过后，没有新改动、失败或未解决的问题就不重复执行或追加无关检查；完成时简述实际验证范围，不将未执行的测试表述为通过。
- 回归使用临时仓库、隔离 Git 配置和模拟 AI 的 `zc_tests`、`zc_backup_tests`。正常应用会读取真实备份并启动监控，不能用来运行自动化回归。
